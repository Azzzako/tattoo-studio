import { defineCloudflareConfig } from '@opennextjs/cloudflare';

export default defineCloudflareConfig({
  // R2, KV or Durable Objects can be wired here later.
  // See https://opennext.js.org/cloudflare for caching options.
});